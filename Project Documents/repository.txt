import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface UserMRepository extends JpaRepository<UserM, String> {
    // 예: 이메일로 유저를 찾는 커스텀 메소드
    // UserM findByEmail(String email);
}

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface SportTypeRepository extends JpaRepository<SportType, String> {
    // 예: 운동 이름으로 찾는 커스텀 메소드
    // SportType findBySportNm(String sportNm);
}

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface AvailableSportsRepository extends JpaRepository<AvailableSports, AvailableSportsId> {
    // 예: 특정 유저 ID로 모든 할 수 있는 운동 목록 찾기
    // List<AvailableSports> findByUserMId(String userId);
}

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface ExerciseHistoryRepository extends JpaRepository<ExerciseHistory, Integer> {
    // 예: 특정 유저의 모든 운동 기록 찾기
    // List<ExerciseHistory> findByUserMId(String userId);
}

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.stereotype.Repository;

@Repository
public interface GpsDataRepository extends JpaRepository<GpsData, Integer> {
    // 예: 특정 운동 기록(History_Id)에 속한 모든 GPS 데이터를 순서(Gps_Seq)대로 정렬하여 찾기
    // List<GpsData> findByExerciseHistoryHistoryIdOrderByGpsSeqAsc(Integer historyId);
}